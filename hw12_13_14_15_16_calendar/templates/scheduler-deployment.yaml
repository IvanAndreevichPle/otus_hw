{{- if .Values.scheduler.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "calendar-chart.fullname" . }}-scheduler
  labels:
    {{- include "calendar-chart.scheduler.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.scheduler.replicaCount }}
  selector:
    matchLabels:
      {{- include "calendar-chart.scheduler.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "calendar-chart.scheduler.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: scheduler
        image: "{{ .Values.global.imageRegistry }}{{ .Values.scheduler.image.repository }}:{{ .Values.scheduler.image.tag }}"
        imagePullPolicy: {{ .Values.scheduler.image.pullPolicy }}
        env:
        - name: CONFIG_FILE
          value: "/etc/calendar/scheduler_config.yaml"
        - name: DB_HOST
          value: {{ .Values.database.host | quote }}
        - name: DB_PORT
          value: {{ .Values.database.port | quote }}
        - name: DB_USER
          value: {{ .Values.database.user | quote }}
        - name: DB_NAME
          value: {{ .Values.database.dbname | quote }}
        - name: RABBITMQ_QUEUE
          value: {{ .Values.rabbitmq.queue | quote }}
        - name: SCHEDULER_INTERVAL
          value: {{ .Values.schedulerConfig.intervalSeconds | quote }}
        - name: LOGGER_LEVEL
          value: {{ .Values.logger.level | quote }}
        volumeMounts:
        - name: config
          mountPath: /etc/calendar
          readOnly: true
        - name: migrations
          mountPath: /migrations
          readOnly: true
        {{- if .Values.scheduler.livenessProbe }}
        livenessProbe:
          {{- toYaml .Values.scheduler.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if .Values.scheduler.readinessProbe }}
        readinessProbe:
          {{- toYaml .Values.scheduler.readinessProbe | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.scheduler.resources | nindent 10 }}
      volumes:
      - name: config
        configMap:
          name: {{ include "calendar-chart.fullname" . }}-scheduler-config
      - name: migrations
        configMap:
          name: {{ include "calendar-chart.fullname" . }}-migrations
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

